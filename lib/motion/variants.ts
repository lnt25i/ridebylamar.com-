/** @deprecated Use @/lib/animations/config — kept for import compatibility */
export {
  DURATION,
  EASE_PREMIUM,
  STAGGER_STEP as staggerStep,
  VIEWPORT_MARGIN,
  VIEWPORT_THRESHOLD,
} from '@/lib/animations/config';

export const viewportOnce = { once: true, margin: '-48px' as const };
